# The committed pnpm-lock.yaml is stale relative to packages/sdk (added later).
# Render's auto-install runs with frozen-lockfile (CI default) and would fail;
# this lets pnpm resolve the missing entry. Remove once the lockfile is regenerated.
frozen-lockfile=false
